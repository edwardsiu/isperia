const { archidekt } = require('./archidekt');
const { scryfall } = require('./scryfall');
const { tappedout } = require('./tappedout');

module.exports = {
    archidekt,
    scryfall,
    tappedout,
};
